{
  "name": "csv-import-readiness-checker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only structural checks for small anonymized CSV samples before CRM import.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
